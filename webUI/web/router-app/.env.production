# Production environment
ENV = 'production'

# base api
VUE_APP_BASE_API = '/prod-api'

# Backend API URL - Use 'auto' to automatically detect the current host
# The actual URL will be constructed at runtime based on window.location
VUE_APP_BACKEND_API = 'auto'
